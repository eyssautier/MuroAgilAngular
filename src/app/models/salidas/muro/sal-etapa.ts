export class SalEtapa {
    id: number;
    nombre: string;
    posicion: number;

    constructor(id: number, nombre: string, posicion: number) {
        this.id = id;
        this.nombre = nombre;
        this.posicion = posicion;
    }
}
